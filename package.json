{
  "name": "recipe-basket",
  "version": "1.0.0",
  "private": true,
  "description": "Find nearby grocery stores that can make a recipe for the lowest estimated basket price.",
  "scripts": {
    "dev": "node local-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
